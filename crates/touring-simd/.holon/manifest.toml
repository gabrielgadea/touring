# schema: /home/gabrielgadea/.claude/tools/holon/holon-manifest.schema.json
# Generated by generate_touring_manifests.py (Fase 1 D1.1). Edit below line.

[holon.identity]
name = "touring-simd"
version = "30.3.0"
description = "SIMD + GPU WGSL compute"
autonomy_guarantee = true

[holon.offers.simd-backend]
schema = "schemas/simd-backend.json"
adapter = "cli"
adapter_cmd = "touring inferlets run classifier"
version = "30.3.0"
# SIMD-accelerated batch compute

[holon.offers.gpu-compute]
schema = "schemas/gpu-compute.json"
adapter = "cli"
adapter_cmd = "touring inferlets run pattern"
version = "30.3.0"
# WGSL GPU shader dispatch

[holon.offers.u4-dot-product]
schema = "schemas/u4-dot-product.json"
adapter = "cli"
adapter_cmd = "touring inferlets run memory"
version = "30.3.0"
# U4 quantized dot product

