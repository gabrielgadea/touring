# schema: /home/gabrielgadea/.claude/tools/holon/holon-manifest.schema.json
# Generated by generate_touring_manifests.py (Fase 1 D1.1). Edit below line.

[holon.identity]
name = "touring-generator"
version = "30.3.0"
description = "Code generation pipeline (30 kinds)"
autonomy_guarantee = true

[holon.offers.code-generation]
schema = "schemas/code-generation.json"
adapter = "cli"
adapter_cmd = "touring generate plan-submit"
version = "30.3.0"
# Full Draft→Committed generator pipeline

[holon.offers.vgp-verification]
schema = "schemas/vgp-verification.json"
adapter = "cli"
adapter_cmd = "touring generate verify"
version = "30.3.0"
# Verified Generation Protocol symbol check

[holon.offers.template-render]
schema = "schemas/template-render.json"
adapter = "cli"
adapter_cmd = "touring generate render"
version = "30.3.0"
# Tera template rendering

[holon.requires.symbol-index]
optional = true

[holon.requires.quality-gate]
optional = true

