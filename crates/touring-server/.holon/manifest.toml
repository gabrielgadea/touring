# schema: /home/gabrielgadea/.claude/tools/holon/holon-manifest.schema.json
# Generated by generate_touring_manifests.py (Fase 1 D1.1). Edit below line.

[holon.identity]
name = "touring-server"
version = "30.3.0"
description = "CLI dispatch + daemon"
autonomy_guarantee = true

[holon.offers.daemon-health]
schema = "schemas/daemon-health.json"
adapter = "cli"
adapter_cmd = "touring doctor"
version = "30.3.0"
# Daemon health probe

[holon.offers.cli-dispatch]
schema = "schemas/cli-dispatch.json"
adapter = "cli"
adapter_cmd = "touring status"
version = "30.3.0"
# Full CLI dispatch status

