# schema: /home/gabrielgadea/.claude/tools/holon/holon-manifest.schema.json
# Generated by generate_touring_manifests.py (Fase 1 D1.1). Edit below line.

[holon.identity]
name = "touring-core"
version = "30.3.0"
description = "Shared primitives + embeddings"
autonomy_guarantee = true

[holon.offers.embedding-service]
schema = "schemas/embedding-service.json"
adapter = "cli"
adapter_cmd = "touring inferlets run memory"
version = "30.3.0"
# Embedding via inferlet memory pool

[holon.offers.u4-quantization]
schema = "schemas/u4-quantization.json"
adapter = "cli"
adapter_cmd = "touring inferlets run classifier"
version = "30.3.0"
# U4 dequantized dot product

