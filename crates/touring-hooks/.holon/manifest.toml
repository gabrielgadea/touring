# schema: /home/gabrielgadea/.claude/tools/holon/holon-manifest.schema.json
# Generated by generate_touring_manifests.py (Fase 1 D1.1). Edit below line.

[holon.identity]
name = "touring-hooks"
version = "30.3.0"
description = "153 hooks registry + runtime"
autonomy_guarantee = true

[holon.offers.pre-edit-gate]
schema = "schemas/pre-edit-gate.json"
adapter = "cli"
adapter_cmd = "touring pre-edit"
version = "30.3.0"
# Pre-edit scored signals composition

[holon.offers.post-edit-quality]
schema = "schemas/post-edit-quality.json"
adapter = "cli"
adapter_cmd = "touring post-edit"
version = "30.3.0"
# Multi-language quality tracker

[holon.offers.health-delta]
schema = "schemas/health-delta.json"
adapter = "cli"
adapter_cmd = "touring health-delta status"
version = "30.3.0"
# Per-path streak + regression tracker

[holon.requires.symbol-index]
optional = true

