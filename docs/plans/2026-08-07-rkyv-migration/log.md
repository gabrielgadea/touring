---
type: Log
title: Log — chronological history of this loop run
description: Append-only history; PreCompact resume notes and phase closes land here.
plan_id: 2026-08-07-rkyv-migration
tags: [loop, log]
timestamp: 2026-08-07T07:19:34.499777-03:00
okf_version: "0.1"
---

# Log

Part of the [bundle](/index.md).

## 2026-08-07T07:27:33.109216-03:00 — P0 done

SPIKE provou por execução que rkyv 0.8.17 RECUSA arquivos escritos pelo 0.7 com erro de validação ('invalid UTF-8'), nunca mis-parse — falha segura. Dois crates mínimos: writer 0.7 (68 bytes, valida o próprio) e reader 0.8 (access::<ArchivedSnapshot, rancor::Error>). Mapa de risco fechado: (a) dependency_cache usa check_archived_root + guarda INDEX_SNAPSHOT_SCHEMA_VERSION -> Err -> regenera; (b) IPC valida por contrato documentado em ipc.rs; (c) os únicos archived_root não-validados são um helper #[cfg(test)] em crdt_graph.rs e um pub unsafe fn read_unchecked em esaa.rs com ZERO chamadores. Conclusão: NÃO existe rota de corrupção silenciosa no swap de versão. Achado extra p/ P2: read_unchecked é órfã (REGRA #0) — remover em vez de migrar.

## 2026-08-07T08:24:11.948503-03:00 — P1 done

FUNIL concluído: 71 substituições em 11 arquivos, zero rkyv:: direto fora de crates/touring-rkyv. Fachada alargada com Infallible e from_bytes (os dois símbolos que faltavam para cobrir a superfície real). touring-storage passou a declarar a fachada. DESCOBERTA POR EXECUÇÃO que refina o P2: derives NÃO podem passar pela fachada — os atributos auxiliares #[archive(...)]/#[archive_attr(...)] só ficam em escopo quando o derive vem do crate original; 15 derives + 1 import revertidos e o motivo documentado no Cargo.toml do touring-storage. Corrigido também o bloqueador pré-existente build_sandbox_wrapper_args (tool_output_router.rs:170): a variante #[cfg(not(feature=tantivy-fts))] tinha 2 parâmetros contra 3 da ativa, quebrando a própria promessa de 'API shape feature-agnostic' — mascarado pela unificação de features (cargo check --workspace passa, subconjunto de 3 crates quebra com E0061). Gates: check --workspace --all-targets exit 0; clippy -D warnings 0 erros; fmt limpo; 15086/15087 na suíte, com a única falha (pre_task_scout_returns_pretooluse_envelope) passando isolada em 10.17s — contenção de daemon sob carga, classe já conhecida, não regressão.

## 2026-08-07T09:04:34.614370-03:00 — PreCompact snapshot

Loop active. Pending: [P2,P3,P4]. Resume: `touring decompose ready task_1786098300356502522`.

## 2026-08-07T10:16:01.480676-03:00 — P2 done

Salto 0.7→0.8 concluído. 9 erros internos da fachada zerados; 21 atributos de derive migrados em 6 arquivos (#[archive]→#[rkyv], check_bytes agora automático); 4 adaptadores (check_archived_root/archived_root/to_bytes/from_bytes) + fn deserialize preservam a forma 0.7 de chamada, então NENHUM dos 40 call sites mudou — só os 5 de Infallible, que não tem contraparte no 0.8. Bound HighSerializer extraído para o trait Serializable (alias por blanket impl): o 0.9 muda uma linha, não cada where. 3 defeitos UPSTREAM verificados na fonte: (a) resolver de ENUM sem doc quebra deny(missing_docs) — resolvido por VISIBILIDADE (submódulo privado), zero allow; (b) RelPtr::emplace fixa rancor::Panic e transforma payload gigante em panic do processo — guardado por MAX_ARCHIVE_BYTES antes de chamar rkyv; (c) from_bytes como pub use cru vazava o 2o genérico para todo consumidor. Gates: check/clippy -D warnings/fmt/rustdoc -D warnings todos exit 0; touring-rkyv 47/47.

## 2026-08-07T10:16:15.596656-03:00 — P3 done

Fio versionado + 2 defeitos que o plano não previa. (1) ENVELOPE: IPC_MAGIC RKYV→RKY2 e SAGA_MAGIC SAGA→SAG2, com IPC_MAGIC_V1/SAGA_MAGIC_V1 guardados só para diagnóstico; dígito de versão no ÚLTIMO byte para não quebrar o despacho por primeiro byte do daemon (R/S). Sem isso a incompatibilidade já falhava alto (bytecheck recusa, o P0 provou), mas com erro 'invalid UTF-8' que aponta corrupção em vez da causa; agora FrameError::BadMagic NOMEIA o peer defasado. Provado por teste, não por asserção: legacy_v1_frame_is_diagnosed_by_name + contraprova unknown_magic_is_not_blamed_on_the_rollout (magic aleatório NÃO pode ser atribuído ao rollout). (2) DUPLO-UNFRAME: handle_saga_request_async lia o header e passava só o CORPO para unframe_saga, que espera o frame INTEIRO — rejeitava TODA mensagem saga. Invisível porque frame_saga não tem cliente de produção. Corrigido espelhando o irmão rkyv (from_bytes direto). (3) ASSIMETRIA C08: daemon.rs comparava magic saga com literal b\"SAGA\" enquanto o irmão rkyv usava a constante — o versionamento teria passado batido ali. CACHES EM DISCO: verificado que TODOS os chamadores de produção de load_rkyv/load_from_mmap degradam graciosamente (if let Ok / match / .ok()); custo do deploy é estado RL/CRDT frio, não quebra.

## 2026-08-07T10:16:34.690284-03:00 — P4 done

Fecho. CARGO DENY VERDE POR CORREÇÃO: 'advisories ok, bans ok, licenses ok, sources ok' exit 0, e deny.toml NÃO contém nenhuma entrada para RUSTSEC-2026-0235 nem para rkyv — a vulnerabilidade saiu, não foi silenciada. Cargo.lock com uma única linha de rkyv (0.8.18); zero resíduo 0.7 na árvore. SUÍTE COMPLETA: 15107/15107 passed (2 flaky), exit 0. DOCS CO-EVOLUÍDAS: README do touring-rkyv (o Usage inteiro ensinava API 0.7 e teria sido copiado por qualquer novo consumidor), lib.rs, templates.rs, ipc.rs, saga_ipc.rs; docs históricos versionados (ARCHITECTURE.v29.5.0, plan-v20) preservados de propósito por descreverem estados passados. LIÇÃO PERSISTIDA: memory key 'rkyv-0.8-upstream-gotchas'. ACHADO COLATERAL CORRIGIDO: PrivateDaemon media prontidão por existência do arquivo de socket, não por resposta — trocado por ida-e-volta (doctor -j até daemon_health ok). MELHOROU MAS NÃO CUROU: b310 antes falhava as 3 tentativas (falha dura), agora falha a 1a e passa na 2a em 10.3s vs 19.8s; segue flaky sob saturação. DÉBITO REPORTADO, NÃO TOCADO (decisão humana): 3 ignores mortos no deny.toml; colisão de nome dos exemplos client_demo; E2E que faz shell-out exercita o binário release VELHO (04/08), logo não cobre esta migração — quem cobre são os testes que linkam as libs.

## 2026-08-07T12:00:27.443644-03:00 — PreCompact snapshot

Loop active. Pending: []. Resume: `touring decompose ready task_1786098300356502522`.

## 2026-08-07T16:20:00-03:00 — P5 done

O contador de órfãos não media dívida: media a falha do próprio medidor. 5031 órfãos sobre 12052 produtores (42%) — proporção implausível. TRÊS defeitos no mesmo resolvedor (`symbol_extractors.rs::resolve_import_path_with_source`), todos corrigidos com teste que falharia antes (11 testes novos): (1) `TOURING_CRATE_MAP` era lista literal de 11 entradas, 5 apontando para diretórios mortos, cobrindo 6 dos 41 crates vivos — agora DERIVADO de `crates/*/Cargo.toml`, o que torna a classe de drift irrepresentável; aliases curtos AMPLIADOS para todos os crates menos `ALIAS_DENY` (o mapa velho aliasava `core` para touring-core/src, a um nome de arquivo de wirear `use core::mem` num crate alheio). (2) Reexportação de fachada não era seguida — `use touring_hooks::tantivy_index::X` percorre 2 saltos com glob; `resolve_reexport` segue, e o resultado ainda passa pela sonda de filesystem, então um `pub use` mal lido devolve None e nunca um caminho fantasma. Aplicado nos DOIS ramos (assimetria C08). (3) `rsplit_once("::")` descartava um nível de módulo real; como `knowledge.rs` E `knowledge/models.rs` coexistem, a aresta pousava num arquivo REAL e ERRADO em vez de falhar. CORREÇÃO DE RELATO: eu havia dito "5 símbolos sinalizados" — eram 82; a cláusula imprime `new[:5]` e li o resumo como o conjunto. FANTASMAS: 301 de 1711 `module_file` apontavam para arquivos inexistentes, invisíveis à varredura de obsoletos (que é dirigida pela tabela `symbols`, e um caminho inventado nunca esteve lá); `purge_module_rows` fecha a lacuna — rebuild real purgou 303. EAGAIN ERA TIMEOUT: `index rebuild` morria em 2m0,058s contra `DAEMON_READ_TIMEOUT_SECS=120`; `WouldBlock` tem Display "Resource temporarily unavailable (os error 11)" e era propagado cru, ao lado da função que existe justamente para não fazer isso. `read_failure` nomeia o timeout e cita `--timeout`; `raise_timeout_floor(1800)` no rebuild, com `compare_exchange` para que a escolha do operador sempre vença — o rebuild completou em 2m20 (3117 arquivos, 0 erros). F2.6 PONTUAVA CONFIGURAÇÃO SEM LER CONFIGURAÇÃO: duas fases sobre um orçamento de 2 MiB com a fonte primeiro; 31,6 MB de fonte esgotavam o teto nos primeiros ~6% e o laço de artefatos nunca rodava — 94 arquivos de config, `ci.yml` e `dependabot.yml` inclusos, jamais lidos pela dimensão BLOCK cujo objeto é configuração insegura. Ordem invertida + orçamento próprio por fase. TETO: `DIR_SCAN_BYTE_CAP` 16 MiB → 128 MiB (corpus 26,9 MB; o teto antigo cobria 59%); objeção de memória medida e descartada (pico ~378 MB em 62 GB); `dir_scan_overflow` anuncia truncagem em `score_scope_native`, ponto único das 14 dims ScopeNative. DEDUP REAL: trait `Verification` ganhou `measure()` obrigatório + `check()` default — 50 caudas idênticas eliminadas e o invariante "todo verificador passa por `finish`" virou estrutural; o refactor EXPÔS 5 verificadores que montavam `DimScore` à mão contornando `finish`, um deles com `suggestions: vec![]` enquanto os irmãos chamavam `auto_remediation`. touring-quality F1.3 0.268 → 0.392. NÃO TOCADO de propósito: bloco repetido 48× de `mod tests` — scaffolding, falso-positivo que a REGRA #0 proíbe maquiar. MEDIDO: órfãos 5031 → 4763; novos-vs-baseline 82 → 63; fantasmas 301 → 0; tier Platinum mantido (composite 0.9386, blockers []); F2.6 1.0000 Pass agora COM evidência. ABERTO COM NÚMERO: F1.3 = 0.5901 (Warn); Pass exige ≥0.80 ≈ 4% de duplicação, cerca de metade das linhas duplicadas atuais — campanha, não passo; oportunidade real identificada e não executada: setup de teste repetido 30× em `touring-dispatch/src/lifecycle/tests.rs`. Dos 63 órfãos novos, 13 sem referência externa; as 4 classes estruturais que um modelo baseado em `use` não enxerga estão em `/knowledge/P5.json`.

## 2026-08-07T17:40:01.745206-03:00 — PreCompact snapshot

Loop active. Pending: []. Resume: `touring decompose ready task_1786098300356502522`.
