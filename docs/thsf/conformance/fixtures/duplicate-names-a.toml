# RFC-001 fixture pair (a): duplicate-names-a + duplicate-names-b share a name.
# Discovery emits warning thsf-manifest-007; each file parses individually OK.

[holon.identity]
name = "same-name"
version = "0.1.0"
description = "Duplicate set — sibling A"
